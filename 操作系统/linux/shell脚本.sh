# ------------------shebang-------------------------
# shebang是一个文本行，其中#!位于解释器路径之前,/bin/bash是Bash的解释器命令路径,bash将以#符号开头的行视为注释。脚本中只有第一行可以使用
# shebang来定义解释该脚本所使用的解释器。

#! /bin/bash

# bash shell已成为当前unix和linux中的标准shell